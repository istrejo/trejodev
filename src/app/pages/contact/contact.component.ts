import { Component } from '@angular/core';

@Component({
  selector: 'app-contact',
  standalone: true,
  template: `
    <div class="min-h-screen flex items-center justify-center">
      <span class="font-mono text-muted text-sm">— contact —</span>
    </div>
  `,
})
export class ContactComponent {}
